# Dockerfile.dev
FROM node:20-alpine

# Set working directory
WORKDIR /app

# Install dependencies first (caches this step unless package.json changes)
COPY package*.json ./
RUN npm install

# Copy the rest of the application code
COPY . .

# Expose the Next.js default port
EXPOSE 3000

# Start the development server
CMD ["npm", "run", "dev"]